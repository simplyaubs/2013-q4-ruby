<h1>Chemical elements</h1>

<ul>
	<% Element.order(:id).each do |element| %>
	<li><%= element.number %>. 
		<a href="/elements/<%= element.id %>">
			<%= element.name %>
		</a>
	</li>
	<% end %>
</ul>
